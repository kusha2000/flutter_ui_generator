{
    "prompt": "Design a retro pixel-style time tracker with top timer, start/pause button, task input, active session card, and scrollable task list.",
    "flutter_code": "import 'package:flutter/material.dart';\n\nclass GeneratedWidget extends StatefulWidget {\n  @override\n  _GeneratedWidgetState createState() => _GeneratedWidgetState();\n}\n\nclass _GeneratedWidgetState extends State<GeneratedWidget> {\n  bool isRunning = false;\n  int seconds = 0;\n  String currentTask = 'Retro Task';\n  TextEditingController taskController = TextEditingController();\n\n  String formatTime(int totalSeconds) {\n    int hours = totalSeconds ~/ 3600;\n    int minutes = (totalSeconds % 3600) ~/ 60;\n    int secs = totalSeconds % 60;\n    return '${hours.toString().padLeft(2, '0')}:${minutes.toString().padLeft(2, '0')}:${secs.toString().padLeft(2, '0')}';\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      backgroundColor: Colors.green[900],\n      appBar: AppBar(\n        title: Text('Time Tracker', style: TextStyle(fontFamily: 'PressStart2P')), \n        backgroundColor: Colors.black,\n        foregroundColor: Colors.white,\n        elevation: 0,\n      ),\n      body: Padding(\n        padding: EdgeInsets.all(20),\n        child: Column(\n          children: [\n            Container(\n              padding: EdgeInsets.all(16),\n              decoration: BoxDecoration(\n                color: Colors.black,\n                border: Border.all(color: Colors.white, width: 4),\n              ),\n              child: Text(\n                formatTime(seconds),\n                style: TextStyle(\n                  fontSize: 36,\n                  fontFamily: 'PressStart2P',\n                  color: Colors.white,\n                ),\n              ),\n            ),\n            SizedBox(height: 20),\n            ElevatedButton(\n              onPressed: () => setState(() => isRunning = !isRunning),\n              style: ElevatedButton.styleFrom(\n                backgroundColor: isRunning ? Colors.red[600] : Colors.green[600],\n                shape: RoundedRectangleBorder(borderRadius: BorderRadius.zero),\n                padding: EdgeInsets.all(16),\n                side: BorderSide(color: Colors.white, width: 2),\n              ),\n              child: Text(\n                isRunning ? 'PAUSE' : 'START',\n                style: TextStyle(fontSize: 16, fontFamily: 'PressStart2P', color: Colors.white),\n              ),\n            ),\n            SizedBox(height: 20),\n            TextField(\n              controller: taskController,\n              decoration: InputDecoration(\n                hintText: 'Task name',\n                filled: true,\n                fillColor: Colors.black,\n                hintStyle: TextStyle(color: Colors.white70, fontFamily: 'PressStart2P'),\n                border: OutlineInputBorder(borderRadius: BorderRadius.zero, borderSide: BorderSide(color: Colors.white, width: 2)),\n              ),\n              style: TextStyle(color: Colors.white, fontFamily: 'PressStart2P'),\n            ),\n            SizedBox(height: 16),\n            if (isRunning)\n              Container(\n                padding: EdgeInsets.all(12),\n                decoration: BoxDecoration(\n                  color: Colors.black,\n                  border: Border.all(color: Colors.white, width: 2),\n                ),\n                child: Row(\n                  children: [\n                    Expanded(\n                      child: Column(\n                        crossAxisAlignment: CrossAxisAlignment.start,\n                        children: [\n                          Text(currentTask, style: TextStyle(fontSize: 14, color: Colors.white, fontFamily: 'PressStart2P')),\n                          Text('${formatTime(seconds)} elapsed', style: TextStyle(color: Colors.white70, fontFamily: 'PressStart2P')),\n                        ],\n                      ),\n                    ),\n                    IconButton(\n                      onPressed: () => setState(() => isRunning = false),\n                      icon: Icon(Icons.stop, color: Colors.red[600]),\n                    ),\n                  ],\n                ),\n              ),\n            SizedBox(height: 16),\n            Expanded(\n              child: ListView.separated(\n                itemCount: 3,\n                separatorBuilder: (context, index) => SizedBox(height: 8),\n                itemBuilder: (context, index) => Container(\n                  color: Colors.black,\n                  child: ListTile(\n                    title: Text('Task ${index + 1}', style: TextStyle(color: Colors.white, fontFamily: 'PressStart2P')),\n                    subtitle: Text('1h ${index * 15}m • Today', style: TextStyle(color: Colors.white70, fontFamily: 'PressStart2P')),\n                  ),\n                ),\n              ),\n            ),\n          ],\n        ),\n      ),\n    );\n  }\n}",
    "category": "productivity",
    "tags": [
        "timer",
        "tracking",
        "productivity",
        "task",
        "retro",
        "pixel"
    ],
    "components": [
        "Scaffold",
        "AppBar",
        "Container",
        "Text",
        "ElevatedButton",
        "TextField",
        "ListView",
        "ListTile",
        "IconButton"
    ],
    "layout_type": "Column"
}