{
    "prompt": "Create a map view page with a full-screen interactive map as the main content. Include a top app bar titled 'Explore Map' with a white background, black text, and a back arrow icon on the left for navigation. Place a floating action button in the bottom-right corner with a GPS icon for locating the user’s current position; use a vibrant orange color (Colors.orange) for the button with a white icon. The map should fill the entire screen below the app bar, with a subtle grey border (2px width) around it. Apply Material Design principles with a clean, minimalistic aesthetic. Ensure the map container has a light grey background (Colors.grey[200]) to indicate the map area.",
    "flutter_code": "import 'package:flutter/material.dart';\n\nclass GeneratedWidget extends StatelessWidget {\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(\n        title: Text('Explore Map'),\n        backgroundColor: Colors.white,\n        foregroundColor: Colors.black,\n        leading: IconButton(\n          icon: Icon(Icons.arrow_back),\n          onPressed: () {},\n        ),\n      ),\n      body: Container(\n        width: double.infinity,\n        height: double.infinity,\n        decoration: BoxDecoration(\n          border: Border.all(color: Colors.grey, width: 2),\n          color: Colors.grey[200],\n        ),\n        child: Center(\n          child: Column(\n            mainAxisAlignment: MainAxisAlignment.center,\n            children: [\n              Icon(\n                Icons.map,\n                size: 100,\n                color: Colors.grey[600],\n              ),\n              SizedBox(height: 16),\n              Text(\n                'Map View Area',\n                style: TextStyle(\n                  fontSize: 18,\n                  color: Colors.grey[600],\n                ),\n              ),\n            ],\n          ),\n        ),\n      ),\n      floatingActionButton: FloatingActionButton(\n        onPressed: () {},\n        backgroundColor: Colors.orange,\n        child: Icon(\n          Icons.my_location,\n          color: Colors.white,\n        ),\n      ),\n    );\n  }\n}",
    "category": "navigation",
    "tags": [
        "map",
        "navigation",
        "location",
        "gps",
        "full-screen",
        "floating-action-button"
    ],
    "components": [
        "Scaffold",
        "AppBar",
        "Container",
        "FloatingActionButton",
        "Icon",
        "Text"
    ],
    "layout_type": "Scaffold"
}